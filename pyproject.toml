[project]
name = "scamtrace"
version = "0.1.0"
description = "Privacy-preserving conversational social-engineering detection"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

